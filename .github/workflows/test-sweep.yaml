name: test-sweep
on: [push]
jobs:
  Install-Dependencies:
    runs-on: ubuntu-latest
    steps:
      - run: npm i
  Build-Application:
    runs-on: ubuntu-latest
    steps:
      - run: npm run build